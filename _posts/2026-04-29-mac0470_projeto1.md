---
title: Projeto 1
date: 2026-04-29 10:00:00 +0000
categories: [MAC0470]
tags: [softaware livre, VM]
---

git pull
git checkout -b <name-of-your-patch-branch> origin/testing
make nconfig
cp .config .config.mpu6050_working.bak (Cria uma cópia com um nome descritivo)
mkdir -p ~/kernel_backups
cp .config ~/kernel_backups/config_iio_mpu6050_$(date +%F)
Fiz as alterações no arquivo
make drivers/iio/imu/inv_mpu6050/
git add drivers/iio/imu/inv_mpu6050/inv_mpu_core.c
git status drivers/iio/imu/inv_mpu6050 (tinha arquivos não queria)
git diff --cached drivers/iio/imu/inv_mpu6050 (mostra os add para commit)
git add --chmod=-x drivers/iio/imu/inv_mpu6050/* (muitos arquivos mudaram de modo old mode 100644 new mode 100755, este comando restaura isso)
git diff --cached (de novo mas na diretório raiz)
git config core.fileMode false (manda o git ignorar mudança de permissões)
git reset (remove todos os adds mas não altera os arquivos alterados)
git add drivers/iio/imu/inv_mpu6050/inv_mpu_core.c
git diff --cached (verficando, deu certo agora)
git commit -s
scripts/checkpatch.pl --git HEAD 
scripts/checkpatch.pl -f drivers/iio/imu/inv_mpu6050/inv_mpu_core.c --fix-inplace
