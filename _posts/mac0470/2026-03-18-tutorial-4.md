---
title: Tutorial 4
permalink: /mac0470/tutorial-4/
tags: [linux, softaware livre, VM]
---

O quarto tutorial foi sobre [**Introduction to Linux kernel Character Device Drivers**](https://flusp.ime.usp.br/kernel/char-drivers-intro/). A seguir, os erros encontrados durante o processo.

## 5) A character device driver example

### Erro de Sintaxe ao Compilar o Kernel

Depois de implementar a função no `simple_char.c` e configurar o `Kconfig` e o `Makefile`, habilitei o módulo com `menuconfig` e executei o comando `kw build`. No entanto a compilação falhou, apresentando uma série de erros de sintaxe que se encontravam no arquivo `uaccess.h`. Ao investigar o arquivo para onde o erro apontava descobri que as funções adicionadas na etapa 3 estavam sem o ponto e vírgula (`;`).

```c
#include <linux/uaccess.h>

unsigned long copy_from_user(void *to, const void __user *from, unsigned long n)
unsigned long copy_to_user(void __user *to, const void *from, unsigned long n)
```

```c
#include <linux/uaccess.h>

unsigned long copy_from_user(void *to, const void __user *from, unsigned long n);
unsigned long copy_to_user(void __user *to, const void *from, unsigned long n);
```